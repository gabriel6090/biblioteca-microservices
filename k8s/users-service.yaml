apiVersion: v1
kind: Service

metadata:
  name: user-service

spec:
  selector:
    app: user-service

  ports:
    - port: 5001
      targetPort: 5001
  
  network:
    - biblioteca-network

  type: ClusterIP