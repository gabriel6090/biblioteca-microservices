apiVersion: v1
kind: Service

metadata:
  name: api-gateway

spec:
  selector:
    app: api-gateway

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30000
  
  network:
    - biblioteca-network

  type: NodePort